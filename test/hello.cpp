#include <iostream>
#include <cstdio>
#include <string.h>
#include <fstream>
using namespace std;

int main() {
    // fstream fout("fout", fstream::out);

    // // if (!fout.is_open()) {
    // //     cerr << "Error: " << strerror(errno) << endl;
    // //     return -1;
    // // }
    // fout << "Hello ";


    printf("Hello World!\n");
    // fout.close();
    return 0;
}